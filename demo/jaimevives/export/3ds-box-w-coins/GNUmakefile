# GNUmakefile generated by Bullet Physics Playground ---------------------------

SCENE = $(shell basename `pwd`)

include ../export.mk

# EOF --------------------------------------------------------------------------
